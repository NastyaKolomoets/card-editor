import { Component, Input } from '@angular/core';
import { ContentLayoutBaseComponent } from '../content-layout-base.component';

@Component({
	selector: 'app-image-overlap',
	templateUrl: 'image-overlap.component.html',
	styleUrls: ['image-overlap.component.css']
})
export class ImageOverlapComponent extends ContentLayoutBaseComponent {
	// @Input() textFields: string[];
	// @Input() imgUrl: string;
}
