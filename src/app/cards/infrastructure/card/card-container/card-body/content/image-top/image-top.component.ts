import { Component, Input } from '@angular/core';
import { ContentLayoutBaseComponent } from '../content-layout-base.component';

@Component({
	selector: 'app-image-top',
	templateUrl: 'image-top.component.html',
	styleUrls: ['image-top.component.css']
})
export class ImageTopComponent extends ContentLayoutBaseComponent {
	// @Input() textFields: string[];
	// @Input() imgUrl: string;
}
