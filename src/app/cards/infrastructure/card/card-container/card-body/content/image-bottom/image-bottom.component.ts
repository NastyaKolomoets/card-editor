import { Component, Input } from '@angular/core';
import { ContentLayoutBaseComponent } from '../content-layout-base.component';

@Component({
	selector: 'app-image-bottom',
	templateUrl: 'image-bottom.component.html',
	styleUrls: ['image-bottom.component.css']
})
export class ImageBottomComponent extends ContentLayoutBaseComponent {
	// @Input() textFields: string[];
	// @Input() imgUrl: string;
}
