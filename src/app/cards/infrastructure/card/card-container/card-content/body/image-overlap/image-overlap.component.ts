import { Component, Input } from '@angular/core';
import { BodyLayoutBaseComponent } from '../body-layout-base.component';

@Component({
	selector: 'app-image-overlap',
	templateUrl: 'image-overlap.component.html',
	styleUrls: ['image-overlap.component.css']
})
export class ImageOverlapComponent extends BodyLayoutBaseComponent {
	// @Input() textFields: string[];
	// @Input() imgUrl: string;
}
