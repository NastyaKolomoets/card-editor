import { Component, Input } from '@angular/core';
import { ContentLayoutBaseComponent } from '../content-layout-base.component';

@Component({
	selector: 'app-image-center',
	templateUrl: 'image-center.component.html',
	styleUrls: ['image-center.component.css']
})
export class ImageCenterComponent extends ContentLayoutBaseComponent {
	// @Input() textFields: string[];
	// @Input() imgUrl: string;
}
